rules:
  - id: demo-secret
    category: general
    title: Demo Secret
    severity: HIGH
    regex: (?i)(?P<key>(TEST_SECRET_TOKEN|secret))(=|:).{0,5}['"](?P<secret>[0-9a-zA-Z\-_=]{8,64})['"]
    keywords:
      - TEST_SECRET_TOKEN
